{
    "version": "2.0.0",
    "type": "shell",
    "tasks": [
        {
            "label": "build",
            "command": "${workspaceRoot}/ src --out-dir dist -w --source-maps",
            "group": "build",
            "isBackground": true,
            "problemMatcher": []
        }
    ]
  }